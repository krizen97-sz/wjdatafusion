# v4.4.1 → v4.8.3 数据库交付验证

日期：2026-09-18。应用基线：`7873ec8`，起始版本：`e19796b`。本记录只对应累计 SQL 的本地验证；未执行远端升级，也不代表应用/引擎/代理部署验收。

## 范围审计

`git diff e19796b..7873ec8 -- '*.sql'` 仅包含新增的：

- `data_governance_upgrade_20260909_v4_5_0.sql`
- `data_governance_upgrade_20260916_v4_4_1_to_v4_8_2_all.sql`
- `data_governance_verify_20260916_v4_8_2.sql`

后两者包装同一菜单/权限增量。v4.8.3 无新的持久化 DDL；`doc_user_quota.max_upload_bytes=0` 由应用解释为个人单文件无上限，本包只核验现有列及数值，不更改用户总空间或单文件限额。旧版 SQL/README/测试均未修改。

## 实测环境

- MySQL 8.4.6，127.0.0.1:13306。
- 连接前由测试校验固定主机/端口及专用运行目录下的 MySQL datadir。
- 每个场景使用本次随机 UUID、`rynew_gov_upgrade_test_` 前缀的新库；只清理本次显式登记的库名。
- 夹具仅包含此次升级涉及的菜单/角色/授权表、文档额度表和不可变哨兵表，不宣称建立了整个 v4.4.1 业务系统。
- 没有读取或修改已有业务库。

执行命令：

```bash
PYTHONDONTWRITEBYTECODE=1 python3 tools/data-governance/tests/test_upgrade_v4_4_1_to_v4_8_3.py --runtime-root /绝对路径/rynew-runtime/data-governance
```

结果：**12 项测试全部通过，用时 48.849 秒**。11 类菜单/角色冲突与 10 类文档问题作为子场景覆盖。

| 项目 | 结果 |
| --- | --- |
| 新升级增加 6 个菜单/按钮、6 项管理员授权 | PASS |
| 重跑数据快照一致 | PASS |
| 已执行 v4.5.0 或 v4.8.2 累计脚本，升级不重复增写 | PASS |
| 合法部分菜单补齐，保留自定义名称 | PASS |
| 保留隐藏/停用、自定义子菜单和其他角色权限 | PASS |
| 菜单路径、路由、组件、权限、角色、基础表冲突时拒绝写入 | PASS |
| 模拟授权 INSERT 报错时，新菜单事务回滚 | PASS |
| 文档表/列缺失、类型错误、无效额度、额外 CHECK/触发器时拒绝写入 | PASS |
| 保留 100 MB、无限制、历史 50 MB 个人额度及其审计字段 | PASS |
| 在测试库证明 signed/unsigned BIGINT 可保存 0 | PASS |
| 正常和缺文档表场景的核验 SQL 均保持数据快照一致 | PASS |
| 误用 --force 时，预检失败仍无永久数据写入 | PASS |

`--force` 场景仅用于证明预检额外防护，正式执行仍禁止使用；中途 DML 失败不能被忽略。升级脚本共用 v4.8.2 命名锁，正常批处理遇错断开连接会回滚事务。工作树模块校验与 `git diff --check` 通过。

MySQL 5.7/其他8.x只作为语法兼容目标，未在这些版本实跑。含额外 CHECK/触发器的现场库须由可查看其元数据的 DBA 单独审核，脚本不自动放行或删除约束。
