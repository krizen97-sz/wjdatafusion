# 华东信息融合平台前端设计规范

本文档用于约束 `wjdatafusion` 中现场融合管理、自动化巡检、文档管理、IP 分配管控和系统运维页面的设计、交互与发布前检查。项目使用 Vue 3 与 Element Plus，本规范只沉淀设计方法和审查规则，不引入 Impeccable 代码、CLI、子模块或运行时依赖。

参考来源：[pbakaus/impeccable](https://github.com/pbakaus/impeccable)。本项目只吸收其中适合后台产品的部分：先明确产品场景与用户任务，再围绕布局、信息层级、文案、响应式、边界状态和发布前检查做审查。

## 1. 产品定位

现场融合管理是一个后台工作台，不是营销页。前端的核心目标是让用户快速看清现场、主平台、子平台、页面、服务器、组织、人员、留言和操作记录之间的关系，并高效完成配置维护。

设计优先级：

1. 信息准确
2. 操作高效
3. 层级清晰
4. 状态明确
5. 视觉克制

页面不追求强装饰感。除非功能本身需要，否则避免大面积插画、过度渐变、复杂动效和营销式英雄区。

## 2. 设计工作流

每次涉及首页、现场画布、弹窗、表格、服务器管理、留言板或版本记录的前端改动，先按下面顺序思考：

1. 明确用户任务：用户打开这个页面是为了查状态、找现场、改配置、导入导出，还是追溯操作记录。
2. 明确主工作区：每个页面只突出一个主要工作区，其他区域提供辅助信息。
3. 整理信息层级：先放决策所需信息，再放详情和低频配置。
4. 确认操作路径：新增、编辑、删除、导入、导出、查看详情、打开画布等动作必须可预期。
5. 检查边界状态：空数据、权限不足、加载失败、长文本、重复数据、窄屏和弹窗高度都要有处理。
6. 发布前自检：按本文档第 9 节执行。

可借鉴 Impeccable 的审查词汇，但要转成本项目语境：

| 审查词 | 本项目含义 |
| --- | --- |
| audit | 检查权限、响应式、可访问性、性能和边界状态 |
| polish | 做最终视觉整理，统一间距、按钮、文案和状态 |
| layout | 调整布局、空间节奏、主次区域和滚动边界 |
| harden | 补齐错误、空态、长文本、权限不足和导入失败场景 |
| distill | 去掉低价值说明、重复卡片和干扰主任务的装饰 |

## 3. 首页工作台

首页对不同权限用户展示不同内容。`datafusion` 用户看到现场融合工作台，`vehiclealarm` 用户保留车辆告警首页，普通用户保留通用首页。

设计要求：

- `datafusion` 首页优先展示“我的相关现场、全站最新修改、现场简表、快捷入口”。
- 首屏不要被欢迎文案占满，标题区应紧凑，统计信息要服务后续操作。
- “新增现场”“进入现场管理”“版本记录”“配置画布”等入口要明确、稳定。
- 我的现场卡片只展示判断和进入画布所需信息，不承载完整详情。
- 全站最新修改按增删改动态组织，不显示普通查询记录。
- 空数据时给出明确下一步，例如“去新增现场”或“进入现场管理”。

避免：

- 通用欢迎页式大段说明。
- 与当前权限无关的模块入口。
- 多套卡片风格混用。
- 把版本号、权限数、角色数做成抢主视觉的大组件。

## 4. 现场关系画布

现场配置画布是现场融合管理的主工作区。画布需要帮助用户理解“现场 -> 主平台 -> 子平台 -> 页面 / 服务器 / 人员”的关联关系。

设计要求：

- 画布区域优先级最高，顶部统计、右侧详情、留言和操作记录都应服务画布。
- 横向树和纵向树切换时，节点间距、连接线、分组标题和操作入口要保持一致。
- 现场与主平台之间的连线必须连续、清晰，新增或删除主平台后要根据当前数据动态重排。
- 人员层、子平台层、服务器层之间要有清晰分隔，但不要形成厚重边框。
- 子平台过多时优先换行和分组，避免单个主平台无限横向拉长。
- 服务器在画布上以数量和入口呈现，详细增删改查集中在服务器管理弹窗中完成。
- 全屏画布中应保留核心操作能力，不让用户退出全屏才能完成配置。

避免：

- 连接线断裂、重叠或与节点视觉无关。
- 右键菜单成为唯一操作入口。
- 画布被侧边面板、统计卡或留言板挤压到不可用。
- 缩放、搜索、全屏等工具按钮过小或位置不稳定。

## 5. 表格、弹窗和抽屉

后台页面优先沿用 Element Plus 与若依现有交互习惯，不重新发明组件体系。

设计要求：

- 表格用于批量扫描，字段顺序按业务判断顺序排列。
- 操作列只保留高频动作，低频动作放入二级弹窗或批量工具中。
- 弹窗只承载一个明确任务，例如新增现场、批量添加服务器、查看操作详情。
- 抽屉适合留言详情、记录详情等可浏览内容，不适合复杂表单。
- 批量确认页必须支持修改、删除、全选、重复标识和最终确认。
- 导入导出类弹窗要明确文件格式、模板、明文密码风险和失败原因。

避免：

- 卡片套卡片。
- 同一弹窗内同时完成多个互不相关任务。
- 高度过大的单条服务器、联系人或记录项。
- 只有图标没有可理解提示的危险操作。

## 6. 文案与状态

所有前端文案都要面向业务动作，而不是描述组件。

推荐：

- “配置画布”
- “导出服务器”
- “显示密码”
- “确认添加”
- “复用已有服务器并绑定到当前子平台”
- “现场融合首页数据加载失败，请稍后重试”

状态处理要求：

- 权限不足：明确说明缺少哪个能力，避免按钮静默消失造成误解。
- 空数据：说明当前没有什么，并给出下一步入口。
- 导入失败：说明文件格式、字段或数据哪一类问题。
- 重复数据：标识已有归属，例如已存在于哪个子平台。
- 长文本：使用换行、Tooltip 或详情弹窗，不挤压按钮。

避免：

- “操作失败”这类无原因提示。
- “明文”这类容易让用户误解为按钮名称的文案，优先使用“显示密码”“导出服务器”。
- 页面上出现解释设计意图的说明文字。

## 7. 视觉规则

本项目视觉应克制、稳定、清楚，适合反复使用。

推荐：

- 使用 Element Plus 的按钮、表格、标签、弹窗、抽屉、分页和提示组件。
- 圆角保持适中，常规面板和卡片不要超过 14px，列表项可以更小。
- 阴影只用于弹窗、浮层和关键区域，不用于每个小块。
- 颜色用于区分状态和网络环境，不用于装饰堆叠。
- 图标用于提升识别速度，按钮仍要保留必要文字。
- 主色数量克制，避免同一页面内多个强主色争抢。

图标使用规范：

- Element Plus 原生控件优先使用 `@element-plus/icons-vue` 中与控件配套的图标。
- 平台导航、业务模块、状态和自定义操作优先复用 `src/assets/icons/svg` 中的现有平台图标；需要补充语义时使用 `keyline-*` 扩展图标。
- 后续前端设计不得为单个页面手工绘制或拼接新 SVG 图标。现有图标没有完全同名语义时，选择最接近且不会误导的通用图标；确需引入新的图标来源时，必须先确认开源许可和平台级复用价值。
- `keyline-*` 图标固定使用本地 Stroke 版本，保持 `24×24`、`currentColor` 和 `2px` 线宽，不在业务页面修改其路径、线宽或圆角。
- 图标只辅助识别。新增、删除、导入、保存、危险操作等按钮仍保留明确文字、Tooltip 或可访问名称，不能只靠图形和颜色表达含义。

避免：

- 大面积紫蓝渐变、纯装饰背景、漂浮装饰图形。
- 灰色文字压在彩色背景上。
- 纯黑、纯灰的大面积界面，应保持轻微色相和层次。
- 过度圆角的图标块、按钮块和统计块。
- 无意义动效、弹跳或过度夸张的过渡。

## 8. 响应式与可访问性

后台系统主要面向桌面，但仍要保证窄屏可用。

要求：

- 文本不能溢出按钮、标签、卡片和表格单元。
- 弹窗高度要限制在视口内，内部内容可滚动。
- 关键工具栏在窄屏下允许换行，不允许互相覆盖。
- 全屏画布、横向树、纵向树都要检查不同宽度下的连线和节点。
- 仅图标按钮需要 Tooltip 或可理解的 `title`。
- 危险操作要有确认反馈，不能只靠颜色表达。

## 9. 平台主题与自动化巡检

平台统一使用语义主题变量，不允许业务页面继续把白色背景、深蓝标题或灰色说明写死在组件中。浅色和深色模式必须共用相同的信息层级、状态含义和交互结构，主题切换只改变表面、文字、边框和图表颜色。

自动化巡检采用“统一健康账本”逻辑：

- 驾驶舱先展示例行巡检与高频健康合并后的当日结论，再展示趋势、计划状态和问题清单。
- 驾驶舱只面向桌面值守场景，保持综合健康带、双列图表、计划健康表、双列问题与记录的稳定阅读顺序和信息密度，不为移动端另做卡片化或重排。
- 巡检总览负责按日查看例行记录和高频健康明细，不再承担完整驾驶舱职责。
- 配置页继续以模板和计划为核心；模板、目标、步骤、计划使用相同的顶部标签、控件高度、错误提示和弹窗滚动规则。
- 健康状态固定为健康、需关注、异常、未检测四类，任何界面都不能只靠颜色区分。
- 图表使用本地 ECharts，颜色读取主题变量；切换主题后必须重新渲染坐标轴、网格、Tooltip 和状态色。
- 401、404、全局加载和路由切换使用平台自身文案与视觉，不展示上游框架动画或外部资源。

## 10. 发布前自检清单

涉及前端页面改动时，至少检查：

- 布局：主工作区是否清楚，页面是否出现无意义滚动或横向撑破。
- 权限：`datafusion`、`vehiclealarm`、普通用户是否进入正确首页；按钮权限是否符合若依权限模型。
- 空态：无现场、无服务器、无留言、无操作记录时是否有下一步入口。
- 错误态：接口失败、导入失败、权限不足时是否有明确提示。
- 长文本：现场名称、平台名称、地址、留言、操作详情是否不会挤压布局。
- 窄屏：工具栏、弹窗、抽屉、表格和画布是否仍可操作。
- 画布：全屏、缩放、搜索、横向树、纵向树、连接线和节点操作是否正常。
- 构建：涉及代码改动时执行 `npm run build:prod`。

只修改 Markdown 文档时，不需要执行前端构建。

## 11. 不做的事

- 不引入 Impeccable 依赖。
- 不新增 `.impeccable` 子模块。
- 不改变平台既有基础能力和 Element Plus 的基础组件体系。
- 不把后台工作台改造成营销页或展示页。
- 不为了装饰增加无业务意义的动效和视觉元素。
