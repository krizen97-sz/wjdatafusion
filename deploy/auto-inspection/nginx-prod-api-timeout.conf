# Include these directives inside the existing `location /prod-api/` block.
# Manual inspections connect to multiple targets serially and return only after
# the full template finishes, so they must not inherit Nginx's 60-second limit.
proxy_connect_timeout 30s;
proxy_read_timeout 600s;
proxy_send_timeout 600s;
