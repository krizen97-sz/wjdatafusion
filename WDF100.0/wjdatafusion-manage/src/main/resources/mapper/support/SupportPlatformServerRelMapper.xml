<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hm.manage.mapper.SupportPlatformServerRelMapper">

    <insert id="insertSupportPlatformServerRel" parameterType="SupportPlatformServerRel" useGeneratedKeys="true" keyProperty="relId">
        insert into sup_platform_server_rel(platform_id, server_id, create_by, create_time)
        values(#{platformId}, #{serverId}, #{createBy}, #{createTime})
    </insert>

    <delete id="deleteSupportPlatformServerRel">
        delete from sup_platform_server_rel where platform_id = #{platformId} and server_id = #{serverId}
    </delete>

    <select id="countByPlatformAndServer" resultType="int">
        select count(1) from sup_platform_server_rel where platform_id = #{platformId} and server_id = #{serverId}
    </select>

    <delete id="deleteByPlatformId" parameterType="Long">
        delete from sup_platform_server_rel where platform_id = #{platformId}
    </delete>

    <delete id="deleteByServerId" parameterType="Long">
        delete from sup_platform_server_rel where server_id = #{serverId}
    </delete>
</mapper>
